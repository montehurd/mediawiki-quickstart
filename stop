#!/bin/bash

source "./config"
source "./common/utility.sh"

stop() {
  docker compose -p mediawiki stop 2>&1 | verboseOrDotPerLine "Docker compose stop"
}

if [[ "$0" == "${BASH_SOURCE[0]}" ]]; then
  stop "$@"
fi
